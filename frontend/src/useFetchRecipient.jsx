import React, { useState } from 'react'

function useFetchRecipient() {
    const [receipientUser, setReceipientUser] = useState(null)
    const [error, setError] = useState(null)
    const recipientId = chat.members.
  return (
    <div>useFetchRecipient</div>
  )
}

export default useFetchRecipient